import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
      flex: 1,
      textAlign: 'center',
      paddingTop: '200px',
    },
    button: {
      fontSize: '78px',
    },
    navigation: {
      fontSize: 40,
      Color: 'black',
    },
});